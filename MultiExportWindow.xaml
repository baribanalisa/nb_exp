<Window x:Class="NeuroBureau.Experiment.MultiExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:NeuroBureau.Experiment"
        Title="Мультиэкспорт"
        Width="1100" Height="700"
        MinWidth="900" MinHeight="600"
        WindowStartupLocation="CenterOwner"
        Loaded="Window_Loaded"
        Style="{DynamicResource ModernWindowStyle}">

    <Window.Resources>
        <SolidColorBrush x:Key="BgBrush" Color="#FFF7F7F9"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="TextBrush" Color="#FF111827"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#FF6B7280"/>
        <SolidColorBrush x:Key="BorderColorBrush" Color="#FFE5E7EB"/>
        <SolidColorBrush x:Key="BorderBrushSoft" Color="#1A000000"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#1D4ED8"/>
        <SolidColorBrush x:Key="AccentPressedBrush" Color="#1E40AF"/>
        <SolidColorBrush x:Key="HoverBrush" Color="#F3F4F6"/>

        <Style x:Key="ModernWindowStyle" TargetType="Window">
            <Setter Property="Background" Value="{StaticResource BgBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
        </Style>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <Style x:Key="FormLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="14,9"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColorBrush}"/>
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BorderColorBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Padding" Value="12,7"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentPressedBrush}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource AccentPressedBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <local:StimulusDisplayNameConverter x:Key="StimulusDisplayNameConverter"/>
        <local:DataFormatConverter x:Key="DataFormatConverter"/>
        <local:ImageFormatConverter x:Key="ImageFormatConverter"/>
        <local:NullToVisibleConverter x:Key="NullToVisibleConverter"/>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Stimuli (с миниатюрами) -->
        <Border Grid.Row="0" Grid.Column="0" Margin="0,0,0,12" Style="{StaticResource CardBorderStyle}">
            <DockPanel LastChildFill="True">
                <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Стимулы" Style="{StaticResource SectionTitleStyle}"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Все"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Click="SelectAllStimuli_Click"
                                Margin="0,0,8,0"/>
                        <Button Content="Снять все"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Click="ClearAllStimuli_Click"/>
                    </StackPanel>
                </Grid>

                <Border Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource BorderColorBrush}"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="6">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding Stimuli}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="140" 
                                            CornerRadius="10" 
                                            Padding="8" 
                                            Margin="4"
                                            Cursor="Hand"
                                            MouseLeftButtonDown="StimulusItem_Click">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#F3F4F6"/>
                                                    </Trigger>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="Background" Value="#E0E7FF"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        
                                        <StackPanel>
                                            <!-- Миниатюра -->
                                            <Grid Height="80" Margin="0,0,0,8">
                                                <Border CornerRadius="8"
                                                        BorderBrush="{StaticResource BorderBrushSoft}"
                                                        BorderThickness="1"
                                                        Background="#F9FAFB"/>
                                                
                                                <Border CornerRadius="8" ClipToBounds="True">
                                                    <Image Source="{Binding Thumbnail}" 
                                                           Stretch="UniformToFill"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"/>
                                                </Border>
                                                
                                                <!-- Иконка если нет превью -->
                                                <TextBlock Text="🖼"
                                                           FontSize="24"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           Foreground="{StaticResource MutedTextBrush}"
                                                           Visibility="{Binding Thumbnail, Converter={StaticResource NullToVisibleConverter}}"/>
                                                
                                                <!-- Галочка выбора -->
                                                <Border HorizontalAlignment="Right" 
                                                        VerticalAlignment="Top"
                                                        Margin="0,4,4,0"
                                                        Width="20" Height="20"
                                                        CornerRadius="10"
                                                        Background="#4F46E5"
                                                        Visibility="{Binding IsSelected, Converter={StaticResource BoolToVisibilityConverter}}">
                                                    <TextBlock Text="✓" 
                                                               Foreground="White" 
                                                               FontSize="12"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center"/>
                                                </Border>
                                            </Grid>
                                            
                                            <!-- Название -->
                                            <TextBlock Text="{Binding DisplayName}"
                                                       FontSize="11"
                                                       TextTrimming="CharacterEllipsis"
                                                       TextAlignment="Center"
                                                       Foreground="{StaticResource TextBrush}"
                                                       ToolTip="{Binding Filename}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </DockPanel>
        </Border>

        <!-- Results (с именами, датами и длительностью) -->
        <Border Grid.Row="0" Grid.Column="2" Margin="0,0,0,12" Style="{StaticResource CardBorderStyle}">
            <DockPanel LastChildFill="True">
                <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Результаты" Style="{StaticResource SectionTitleStyle}"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Все"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Click="SelectAllResults_Click"
                                Margin="0,0,8,0"/>
                        <Button Content="Снять все"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Click="ClearAllResults_Click"/>
                    </StackPanel>
                </Grid>

                <Border Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource BorderColorBrush}"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="6">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding Results}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="10" 
                                            Padding="12,10" 
                                            Margin="2"
                                            Cursor="Hand"
                                            MouseLeftButtonDown="ResultItem_Click">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#F3F4F6"/>
                                                    </Trigger>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="Background" Value="#E0E7FF"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- Checkbox -->
                                            <CheckBox Grid.Column="0" 
                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,10,0"/>
                                            
                                            <!-- Имя участника -->
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding DisplayName}"
                                                           FontWeight="SemiBold"
                                                           FontSize="13"
                                                           Foreground="{StaticResource TextBrush}"
                                                           TextTrimming="CharacterEllipsis"
                                                           ToolTip="{Binding Uid}"/>
                                            </StackPanel>
                                            
                                            <!-- Дата -->
                                            <TextBlock Grid.Column="2"
                                                       Text="{Binding DateText}"
                                                       FontSize="12"
                                                       Foreground="{StaticResource MutedTextBrush}"
                                                       VerticalAlignment="Center"
                                                       Margin="10,0"/>
                                            
                                            <!-- Длительность -->
                                            <Border Grid.Column="3"
                                                    Background="#F3F4F6"
                                                    CornerRadius="4"
                                                    Padding="8,4"
                                                    VerticalAlignment="Center">
                                                <TextBlock Text="{Binding DurationText}"
                                                           FontSize="11"
                                                           FontFamily="Consolas"
                                                           Foreground="{StaticResource TextBrush}"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </DockPanel>
        </Border>

        <!-- Options panel -->
        <Border Grid.Row="1" Grid.ColumnSpan="3" Style="{StaticResource CardBorderStyle}" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Папка" Style="{StaticResource FormLabelStyle}"/>
                <TextBox Grid.Column="1"
                         Text="{Binding OutputDir, UpdateSourceTrigger=PropertyChanged}"
                         MinWidth="520"/>
                <Button Grid.Column="2"
                        Content="…"
                        Width="44"
                        Margin="10,0,0,0"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Click="BrowseOutputDir_Click"/>

                <TextBlock Grid.Row="1" Text="Шаблон" Style="{StaticResource FormLabelStyle}" Margin="0,10,0,0"/>
                <Grid Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             Text="{Binding FilenameTemplate, UpdateSourceTrigger=PropertyChanged}"
                             MinWidth="520"/>

                    <TextBlock Grid.Column="1"
                               Text="{Binding TemplateStatusEmoji}"
                               FontSize="16"
                               Margin="10,0,0,0"
                               VerticalAlignment="Center"/>

                    <TextBlock Grid.Column="2"
                               Text="{Binding TemplateStatusText}"
                               Margin="10,0,0,0"
                               VerticalAlignment="Center"
                               Foreground="{StaticResource MutedTextBrush}"/>
                </Grid>

                <TextBlock Grid.Row="2" Text="Режим" Style="{StaticResource FormLabelStyle}" Margin="0,10,0,0"/>
                <Border Grid.Row="2" Grid.Column="1" Margin="0,10,0,0"
                        Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource BorderColorBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Width="260"
                        HorizontalAlignment="Left">
                    <ComboBox ItemsSource="{Binding Modes}"
                              SelectedItem="{Binding Mode}"
                              BorderThickness="0"
                              Background="Transparent"
                              Padding="10,8">
                        <ComboBox.Resources>
                            <Style TargetType="ComboBoxItem">
                                <Setter Property="Padding" Value="10,8"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F3F4F6"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E5E7EB"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ComboBox.Resources>
                    </ComboBox>
                </Border>

                <!-- Формат данных -->
                <TextBlock Grid.Row="3" Text="Формат данных" Style="{StaticResource FormLabelStyle}" Margin="0,10,0,0"/>
                <Border Grid.Row="3" Grid.Column="1" Margin="0,10,0,0"
                        Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource BorderColorBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Width="260"
                        HorizontalAlignment="Left">
                    <ComboBox ItemsSource="{Binding DataFormats}"
                              SelectedItem="{Binding DataFormat}"
                              BorderThickness="0"
                              Background="Transparent"
                              Padding="10,8">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource DataFormatConverter}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                        <ComboBox.Resources>
                            <Style TargetType="ComboBoxItem">
                                <Setter Property="Padding" Value="10,8"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F3F4F6"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E5E7EB"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ComboBox.Resources>
                    </ComboBox>
                </Border>

                <!-- Формат изображений -->
                <TextBlock Grid.Row="4" Text="Формат изображений" Style="{StaticResource FormLabelStyle}" Margin="0,10,0,0"/>
                <Border Grid.Row="4" Grid.Column="1" Margin="0,10,0,0"
                        Background="{StaticResource SurfaceBrush}"
                        BorderBrush="{StaticResource BorderColorBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Width="260"
                        HorizontalAlignment="Left">
                    <ComboBox ItemsSource="{Binding ImageFormats}"
                              SelectedItem="{Binding ImageFormat}"
                              BorderThickness="0"
                              Background="Transparent"
                              Padding="10,8">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource ImageFormatConverter}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                        <ComboBox.Resources>
                            <Style TargetType="ComboBoxItem">
                                <Setter Property="Padding" Value="10,8"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F3F4F6"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E5E7EB"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ComboBox.Resources>
                    </ComboBox>
                </Border>

                <Grid Grid.Row="5" Grid.ColumnSpan="3" Margin="0,14,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <WrapPanel Grid.Column="0" VerticalAlignment="Center">
                        <CheckBox Content="Исходные данные (bin)" IsChecked="{Binding ExportSource}" IsEnabled="{Binding CanExportRawOrSource}" Margin="0,0,14,10"/>
                        <CheckBox Content="Сырые данные взгляда" IsChecked="{Binding ExportRaw}" IsEnabled="{Binding CanExportRawOrSource}" Margin="0,0,14,10"/>
                        <CheckBox Content="Действия" IsChecked="{Binding ExportActions}" IsEnabled="{Binding CanExportRawOrSource}" Margin="0,0,14,10"/>
                        <CheckBox Content="Области интереса" IsChecked="{Binding ExportAoi}" Margin="0,0,14,10"/>
                        <CheckBox Content="Карта движения взгляда" IsChecked="{Binding ExportGazeImage}" IsEnabled="{Binding CanExportImages}" Margin="0,0,14,10"/>
                        <CheckBox Content="Тепловая карта" IsChecked="{Binding ExportHeatImage}" IsEnabled="{Binding CanExportImages}" Margin="0,0,14,10"/>
                        <CheckBox Content="EDF" IsChecked="{Binding ExportEdf}" IsEnabled="{Binding CanExportEdf}" Margin="0,0,14,10"/>
                    </WrapPanel>

                    <Button Grid.Column="1"
                            Content="Экспорт"
                            Click="Export_Click"
                            Width="120"
                            Height="36"
                            Style="{StaticResource PrimaryButtonStyle}"
                            IsEnabled="{Binding CanStartExport}"/>
                </Grid>

                <TextBlock Grid.Row="6" Grid.ColumnSpan="3" Margin="0,10,0,0"
                           Foreground="{StaticResource TextBrush}"
                           Text="{Binding StatusText}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
